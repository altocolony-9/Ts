/*
class BasePerson{
    protected name: string;
    protected age :number;

    constructor(name: string, age:number){
        this.name = name;
        this.age = age;
    }
    show() :string{
        return `${this.age}は${this.name}歳です。`;
    }
}

class BusinessPerson extends BasePerson{
    work():string{
        return `${this.name}はよく働きます。`;
    }
}

let person = new BusinessPerson("恵一郎", 26);
console.log(person.show());
console.log(person.work());*/ 
